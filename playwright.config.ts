import { defineConfig, devices } from "@playwright/test"

// E2E / live checks run against Playwright's bundled Chromium (version-pinned,
// reproducible), per AGENTS.md. Install once with `npx playwright install chromium`.
// Specs live in ./e2e (added when features land).
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  reporter: "list",
  use: {
    baseURL: "http://localhost:3000",
    trace: "on-first-retry",
  },
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
  ],
  webServer: {
    command: "npm run dev",
    url: "http://localhost:3000",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
})
